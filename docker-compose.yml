version: '3.4'
services:
  thewaytogerman.front:
    image: thewaytogermanfront
    restart: on-failure
    ports:    
      - 6080:3000    
    build:
      context: .
      dockerfile: Dockerfile
    
